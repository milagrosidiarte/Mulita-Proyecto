import { EventEmitter } from 'events';
import type { IfNever, StrictOmit } from 'ts-gems';
export type EventMap<T> = Record<keyof T, AnyRest | Listener>;
type AnyRest = [...args: any[]];
type Listener<P extends any[] = any[]> = (...args: P) => any;
type IfEventMap<T extends EventMap<T> | never, True, False> = IfNever<T, False, {} extends T ? False : True>;
type EventNames<T extends EventMap<T> | never, K extends keyof T | string | symbol> = IfEventMap<T, K | (keyof T & (string | symbol)) | keyof EventEmitterEventMap, string | symbol>;
type ParametersOf<T extends EventMap<T> | never, K extends keyof T | string | symbol> = IfEventMap<T, K extends keyof T ? AnyRest : AnyRest, AnyRest>;
type ListenerOf<T extends EventMap<T> | never, K extends string | symbol> = IfEventMap<T, K extends keyof T ? Listener : Listener, Listener>;
interface EventEmitterEventMap {
    newListener: [eventName: string | symbol, listener: Listener];
    removeListener: [eventName: string | symbol, listener: Listener];
}
export declare class AsyncEventEmitter<T extends EventMap<T> | never = never> {
    constructor();
    emitSafe<K extends string | symbol>(event: EventNames<T, K>, ...args: ParametersOf<T, K>): boolean;
    /**
     * Asynchronously calls each of the listeners registered for the event named `eventName`,
     * in the order they were registered, passing the supplied arguments to each.
     * @param event
     * @param args
     */
    emitAsync<K extends string | symbol>(event: EventNames<T, K>, ...args: ParametersOf<T, K>): Promise<boolean>;
    /**
     * Asynchronously calls each of the listeners registered for the event named `eventName`,
     * in the order they were registered, passing the supplied arguments to each.
     * @param event
     * @param args
     */
    emitAsyncSafe<K extends string | symbol>(event: EventNames<T, K>, ...args: ParametersOf<T, K>): Promise<boolean>;
    /**
     * In a serial order, asynchronously calls each of the listeners registered for the event named `eventName`,
     * in the order they were registered, passing the supplied arguments to each.
     * @param event
     * @param args
     */
    emitAsyncSerial<K extends string | symbol>(event: EventNames<T, K>, ...args: ParametersOf<T, K>): Promise<boolean>;
    /**
     * In a serial order, asynchronously calls each of the listeners registered for the event named `eventName`,
     * in the order they were registered, passing the supplied arguments to each.
     * @param event
     * @param args
     */
    emitAsyncSerialSafe<K extends string | symbol>(event: EventNames<T, K>, ...args: ParametersOf<T, K>): Promise<boolean>;
}
export interface AsyncEventEmitter<T extends EventMap<T> | never = never> extends StrictOmit<EventEmitter, 'addListener' | 'prependListener' | 'prependOnceListener' | 'removeListener' | 'removeAllListeners' | 'once' | 'on' | 'off' | 'emit' | 'eventNames' | 'listenerCount' | 'listeners' | 'rawListeners' | 'getMaxListeners' | 'setMaxListeners'> {
    addListener<U extends string | symbol>(event: U, listener: ListenerOf<T, U>): this;
    prependListener<U extends string | symbol>(event: U, listener: ListenerOf<T, U>): this;
    prependOnceListener<U extends string | symbol>(event: U, listener: ListenerOf<T, U>): this;
    removeListener<U extends string | symbol>(event: U, listener: ListenerOf<T, U>): this;
    removeAllListeners(event?: keyof T): this;
    once<U extends string | symbol>(event: U, listener: ListenerOf<T, U>): this;
    on<U extends string | symbol>(event: U, listener: ListenerOf<T, U>): this;
    off<U extends string | symbol>(event: U, listener: ListenerOf<T, U>): this;
    emit<K extends string | symbol>(event: EventNames<T, K>, ...args: ParametersOf<T, K>): boolean;
    eventNames<U extends string | symbol>(): U[];
    listenerCount(type: keyof T): number;
    listeners<U extends string | symbol>(type: U): ListenerOf<T, U>[];
    rawListeners<U extends string | symbol>(type: U): ListenerOf<T, U>[];
    getMaxListeners(): number;
    setMaxListeners(n: number): this;
}
export {};
